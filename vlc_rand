#!/bin/bash

DIR=$(arg_if_null "$1" ".")
COUNT=$(arg_if_null "$2" 1)
EXTENSIONS=$(arg_if_null "$3" "mp4 mkv wmv avi")
PLAYLIST=playlist.m3u

(find_files "$DIR" "$EXTENSIONS" 2> /dev/null | shuf -n $COUNT) > "$PLAYLIST"
vlc_enqueue "$PLAYLIST" 2> /dev/null
rm -f "$PLAYLIST"
